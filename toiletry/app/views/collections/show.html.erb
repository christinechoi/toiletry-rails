<h1>Collection#show</h1>

<h2>Collection: <%= @collection.title %></h2>

  <% @collection_items.each_with_index do |item, index| %>
    <%= index + 1 %>. <%= link_to "#{item.brand.name} #{item.name}", collection_item_path(collection_id: @collection.id, id: item.id) %><br>
  <% end%>


<br>
<br>

<h2><%= link_to 'Add Item to Collection', new_collection_item_path(@collection.id) %></h2>

<br>
<br>
<%= link_to 'Back to Collections', collections_path %>

<p>Find me in app/views/collection/show.html.erb</p>
